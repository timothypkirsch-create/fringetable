name: Facebook Page Test Once (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-time Facebook test trigger disabled after connection check."
