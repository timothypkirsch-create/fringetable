name: Region Audit
on:
  push:
    paths:
      - 'scripts/audit-region-counts.mjs'
permissions:
  contents: read
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Count recipes by region
        run: node scripts/audit-region-counts.mjs
