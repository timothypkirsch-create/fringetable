import fs from 'node:fs';

const date='2026-09-15';
const updates={
 'myanmar-lahpet-thoke':`<section class="recipe-section story-note"><span class="eyebrow">Story &amp; history</span><h2>Tea leaves as food, hospitality and contrast</h2><p>In Myanmar, tea is not confined to the cup. Fermented lahpet is eaten, and lahpet thoke turns it into a composed salad in which the concentrated leaves are balanced by cabbage, tomato, nuts, seeds, fried garlic and crisp legumes.</p><p>Published Burmese accounts connect lahpet with hospitality and social occasions, but the salad is also ordinary contemporary food. Its identity lies in contrast: soft fermented leaves against crunchy accompaniments, bitterness against lime and chile, and rich fried toppings against fresh vegetables.</p><p class="story-caveat">The seasoning and intensity of prepared lahpet vary by producer and household. Taste the leaves before adding lime, fish sauce or salt; a fixed amount can overwhelm an already seasoned package.</p></section>`,
 'nigerian-moi-moi':`<section class="recipe-section story-note"><span class="eyebrow">Story &amp; history</span><h2>A steamed bean dish with many forms</h2><p>Moi moi transforms peeled beans into something entirely different from a whole-bean stew: a smooth, aerated batter that sets through moist heat. It is widely eaten in Nigeria with rice, pap, bread or fried plantain and appears at both everyday meals and larger gatherings.</p><p>Leaves are more than packaging in versions steamed traditionally; they shape the parcel and contribute aroma. Ramekins, foil cups and other covered containers are common practical alternatives, but they do not produce exactly the same sensory result.</p><p class="story-caveat">Spellings include moi moi, moin moin and moyin-moyin. Peppers, oils, proteins, eggs, fish and wrapping methods vary widely, so this accessible ramekin version should be understood as one branch rather than a definitive national formula.</p></section>`
};
for(const [slug,story] of Object.entries(updates)){
 const file=`recipes/${slug}.html`;let html=fs.readFileSync(file,'utf8');
 if(!html.includes('Story &amp; history'))html=html.replace(/(<section class="recipe-section"><span class="eyebrow">Fringe Table adaptation[\s\S]*?<\/section>)/,'$1'+story);
 const m=html.match(/<script type="application\/ld\+json">([\s\S]*?)<\/script>/);const ld=JSON.parse(m[1]);ld.dateModified=date;html=html.replace(m[0],`<script type="application/ld+json">${JSON.stringify(ld)}</script>`);fs.writeFileSync(file,html);
}
let sm=fs.readFileSync('sitemap.xml','utf8');for(const slug of Object.keys(updates))sm=sm.replace(new RegExp(`(<loc>https://fringetable\\.com/recipes/${slug}\\.html</loc><lastmod>)[^<]+`),`$1${date}`);fs.writeFileSync('sitemap.xml',sm);console.log('Added two missing story sections.');
